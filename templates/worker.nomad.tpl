[[/* worker.nomad.tpl — consolidated architecture marker.
The worker task group has been consolidated into web.nomad.tpl under job "<job_name>-web" (fix #405).
This file intentionally renders nothing to prevent duplicate job declarations.
*/]]
